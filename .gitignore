# Build artifacts (regenerated by build.sh)
build/
*.dmg
*.app
*.icns

# macOS cruft
.DS_Store

# ROMs — NEVER commit copyrighted game files.
# The repo ships the emulator; users supply their own games.
*.gb
*.gbc
*.zip
ROMs/
